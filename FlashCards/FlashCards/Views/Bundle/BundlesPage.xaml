<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlashCards.Views.BundlesPage">

    <StackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label  Grid.Column="0" Grid.Row="0" Text="Name"/>
            <Label  Grid.Column="1" Grid.Row="0" Text="Type"/>
            <Label Grid.Column="2" Grid.Row="0" Text=""/>
        </Grid>

        <ListView x:Name="BundlesView"
            ItemsSource="{Binding Bundles}"
            ItemTapped="Handle_ItemTapped"
            CachingStrategy="RecycleElement">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                        <Label Grid.Column="0" Text="{Binding Name}" />
                        <Label  Grid.Column="1" Text="{Binding CardType}" />
                        <Button Grid.Column="2" Text="x" BackgroundColor="OrangeRed"
                                Clicked="DeleteBundle"/>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>


            <ListView.Footer>
                <Button Text="+" Command="{Binding AddBundleCommand}"/>
            </ListView.Footer>
        </ListView>
    </StackLayout>
</ContentPage>
