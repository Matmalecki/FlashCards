<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlashCards.Views.Cards.PhotoCardDetailPage"
             xmlns:local="clr-namespace:FlashCards.Custom">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:StreamToStringConverter x:Key="converter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Grid Padding="5" HeightRequest="200">
                <Image HorizontalOptions="CenterAndExpand" x:Name="ImageDisplay" />
            </Grid>
            <StackLayout HorizontalOptions="Center">
                <Button Text="Pick Photo" Clicked="PickPhotoHandler"/>
                <Label Text="Answer" />
                <Entry Text="{Binding Answer}" x:Name="EntryAnswer" />
                <Button  Text="Save" Clicked="SaveButtonHandler"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>